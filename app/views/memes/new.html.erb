<div class="memeForm">
	<h1>Describe Your Meme</h1>
	
	<%= simple_form_for @memestar, url: memes_path do |f| %>
		<%= f.input :description %>
		<%= f.input :author %>
		<br>
		<%= f.submit 'Create', :class => 'btn btn-danger' %>
	<% end %>
</div>